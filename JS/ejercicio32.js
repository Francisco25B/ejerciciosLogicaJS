// Actividad: Crear una función flecha que multiplique por 2 los elementos de un array.
let duplicarNumeros = (array) => array.map(num => num * 2);
console.log("Array duplicado:", duplicarNumeros([1, 2, 3, 4]));
